import CustomComplete from "@/components/organism/CustomComplete";

const CompleteTemplate = () => {
  return (
    <div>
      <CustomComplete></CustomComplete>
    </div>
  );
};

export default CompleteTemplate;
